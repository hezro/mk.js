name: Snyk Code Test from reusable workflow


on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]


jobs:
  code-scan:
    uses: ${{ github.workspace }}/.github/workflows/test-gha.yml@master
    secrets:
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
